# Streamlit Cloud 部署教程（详细版）

## 🎯 目标

将您的 Streamlit 机器学习应用部署到 Streamlit Cloud，实现：
- ✅ 在线访问
- ✅ 自动部署
- ✅ 免费托管
- ✅ HTTPS 支持

---

## 📋 第一步：准备 GitHub 仓库

### 1.1 创建 GitHub 账号（如果还没有）

1. 访问 https://github.com
2. 点击右上角 "Sign up"
3. 填写信息并创建账号

### 1.2 创建新仓库

1. 登录 GitHub
2. 点击右上角 "+" -> "New repository"
3. 填写信息：
   - **Repository name**: `streamlit-ml-app` (可自定义)
   - **Description**: `机器学习模型可视化系统`
   - **Visibility**: 选择 **Public** (免费版需要公开)
   - **不要**勾选 "Add a README file"
4. 点击 "Create repository"

### 1.3 获取仓库地址

创建成功后，复制仓库地址，例如：
```
https://github.com/你的用户名/streamlit-ml-app.git
```

---

## 📤 第二步：上传代码到 GitHub

### 方法一：使用 Git 命令行（推荐）

#### 2.1 打开命令行

- **Windows**: 按 `Win + R`，输入 `powershell`，回车
- **Mac**: 打开 Terminal
- **Linux**: 打开 Terminal

#### 2.2 进入项目目录

```bash
cd "d:\Python Program\bisai\streamlit_cloud_deploy"
```

#### 2.3 初始化 Git 仓库

```bash
git init
```

#### 2.4 添加所有文件

```bash
git add .
```

#### 2.5 提交文件

```bash
git commit -m "Initial commit: Streamlit ML App"
```

#### 2.6 连接远程仓库

```bash
git remote add origin https://github.com/你的用户名/streamlit-ml-app.git
```

> ⚠️ **注意**: 将 `你的用户名` 替换为您的 GitHub 用户名

#### 2.7 推送代码

```bash
git branch -M main
git push -u origin main
```

#### 2.8 输入账号信息

- 如果提示输入用户名，输入您的 GitHub 用户名
- 如果提示输入密码，需要输入 **Personal Access Token**（不是密码）

#### 2.9 创建 Personal Access Token

如果还没有 Personal Access Token：

1. 登录 GitHub
2. 点击右上角头像 -> "Settings"
3. 左侧菜单找到 "Developer settings"
4. 点击 "Personal access tokens" -> "Tokens (classic)"
5. 点击 "Generate new token" -> "Generate new token (classic)"
6. 填写信息：
   - **Note**: `Streamlit Cloud Deploy`
   - **Expiration**: 选择过期时间（建议 90 天或更久）
   - **Scopes**: 勾选 `repo` (全部权限)
7. 点击 "Generate token"
8. **复制 token**（只显示一次，务必保存）
9. 在命令行中使用这个 token 作为密码

---

### 方法二：使用 GitHub Desktop（图形界面，更简单）

#### 2.1 下载 GitHub Desktop

1. 访问 https://desktop.github.com
2. 下载并安装 GitHub Desktop

#### 2.2 登录 GitHub

1. 打开 GitHub Desktop
2. 点击 "Sign in to GitHub.com"
3. 登录您的 GitHub 账号

#### 2.3 添加本地仓库

1. 点击 "File" -> "Add local repository"
2. 点击 "Choose..."
3. 选择 `streamlit_cloud_deploy` 文件夹
4. 点击 "Add repository"

#### 2.4 发布到 GitHub

1. 点击 "Publish repository"
2. 填写信息：
   - **Name**: `streamlit-ml-app` (可自定义)
   - **Description**: `机器学习模型可视化系统`
   - **Keep this code private**: **取消勾选**（公开仓库）
3. 点击 "Publish repository"

---

### 方法三：使用网页直接上传（最简单，但文件多时不推荐）

#### 2.1 压缩文件夹

1. 右键点击 `streamlit_cloud_deploy` 文件夹
2. 选择 "压缩" 或 "添加到压缩文件"
3. 创建 ZIP 文件

#### 2.2 上传到 GitHub

1. 在 GitHub 仓库页面
2. 点击 "uploading an existing file"
3. 将 ZIP 文件解压后的所有文件拖拽到网页
4. 输入提交信息：`Initial commit: Streamlit ML App`
5. 点击 "Commit changes"

> ⚠️ **注意**: 如果文件很大（>100MB），建议使用 Git 命令行或 GitHub Desktop

---

## 🚀 第三步：部署到 Streamlit Cloud

### 3.1 访问 Streamlit Cloud

1. 访问 https://streamlit.io/cloud
2. 点击 "Sign up" 或 "Get started"

### 3.2 使用 GitHub 登录

1. 点击 "Continue with GitHub"
2. 授权 Streamlit Cloud 访问您的 GitHub 账号
3. 点击 "Authorize streamlit"

### 3.3 新建应用

1. 点击 "New app"
2. 填写应用信息：
   - **Repository**: 选择您刚创建的仓库（`streamlit-ml-app`）
   - **Branch**: 选择 `main`（或 `master`）
   - **Main file path**: 输入 `app.py`
   - **App URL**: 可以自定义（可选，例如：`my-ml-app`）
3. 点击 "Deploy!"

### 3.4 等待部署

1. Streamlit Cloud 会自动：
   - 克隆您的仓库
   - 安装依赖包（从 `requirements.txt`）
   - 启动应用
2. 部署过程通常需要 **2-5 分钟**
3. 您可以在页面上看到部署日志

### 3.5 访问应用

1. 部署完成后，您会看到：
   - ✅ "Your app is live!"
   - 应用 URL，例如：`https://my-ml-app.streamlit.app`
2. 点击 URL 访问您的应用
3. 应用已经可以正常使用了！

---

## 🔍 第四步：验证部署

### 4.1 检查应用功能

1. **访问应用**: 打开 Streamlit Cloud 提供的 URL
2. **测试功能**:
   - 检查页面是否正常加载
   - 测试数据探索功能
   - 测试模型评估功能
   - 测试模型预测功能

### 4.2 检查日志

1. 在 Streamlit Cloud 控制台中
2. 点击应用名称
3. 查看 "Logs" 标签页
4. 检查是否有错误信息

### 4.3 常见问题排查

#### 问题 1: 应用无法加载

**可能原因**:
- 依赖包安装失败
- 代码有错误
- 文件路径不正确

**解决方法**:
1. 查看日志，找到错误信息
2. 检查 `requirements.txt` 是否正确
3. 检查代码是否有语法错误
4. 检查文件路径是否正确

#### 问题 2: 模型文件找不到

**可能原因**:
- 模型文件未上传
- 模型文件路径不正确

**解决方法**:
1. 检查 `models/` 文件夹是否包含所有模型文件
2. 检查 `app.py` 中的模型路径
3. 确保模型文件已提交到 GitHub

#### 问题 3: 数据文件加载失败

**可能原因**:
- 数据文件未上传
- 数据文件路径不正确

**解决方法**:
1. 检查 `去噪后数据.xlsx` 是否已上传
2. 检查 `app.py` 中的数据文件路径
3. 确保数据文件已提交到 GitHub

---

## 🔄 第五步：更新应用

### 5.1 修改代码

1. 在本地修改代码
2. 保存文件

### 5.2 提交更改

#### 使用 Git 命令行：

```bash
cd "d:\Python Program\bisai\streamlit_cloud_deploy"
git add .
git commit -m "Update: 描述您的更改"
git push
```

#### 使用 GitHub Desktop：

1. 打开 GitHub Desktop
2. 左侧会显示更改的文件
3. 填写提交信息
4. 点击 "Commit to main"
5. 点击 "Push origin"

### 5.3 自动部署

1. Streamlit Cloud 会自动检测到更改
2. 自动重新部署应用
3. 通常在 **1-2 分钟**内完成
4. 您可以在 Streamlit Cloud 控制台看到部署状态

---

## 🎨 第六步：自定义应用（可选）

### 6.1 修改应用 URL

1. 在 Streamlit Cloud 控制台中
2. 点击应用名称
3. 进入 "Settings"
4. 修改 "App URL"
5. 点击 "Save"

### 6.2 设置环境变量

1. 在 Streamlit Cloud 控制台中
2. 点击应用名称
3. 进入 "Settings" -> "Secrets"
4. 添加环境变量（如果需要）
5. 点击 "Save"

### 6.3 查看使用统计

1. 在 Streamlit Cloud 控制台中
2. 点击应用名称
3. 查看 "Usage" 标签页
4. 查看应用使用情况

---

## 📊 部署流程图

```
┌─────────────────┐
│  准备项目文件    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  创建GitHub仓库  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  上传代码到GitHub│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 登录Streamlit Cloud│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   创建新应用     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   等待部署完成   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   访问应用URL    │
└─────────────────┘
```

---

## ✅ 部署检查清单

部署前请确认：

- [ ] GitHub 账号已注册
- [ ] Git 已安装（如果使用命令行）
- [ ] 所有文件已准备好
- [ ] `requirements.txt` 文件完整
- [ ] `.streamlit/config.toml` 文件存在
- [ ] 模型文件已包含（`models/*.pkl`）
- [ ] 数据文件已包含（`去噪后数据.xlsx`）
- [ ] 代码已推送到 GitHub
- [ ] Streamlit Cloud 账号已注册
- [ ] 应用已部署
- [ ] 应用可以正常访问
- [ ] 所有功能正常

---

## 🆘 获取帮助

### 文档资源

1. **Streamlit Cloud 文档**: https://docs.streamlit.io/streamlit-community-cloud
2. **Streamlit 文档**: https://docs.streamlit.io
3. **Git 文档**: https://git-scm.com/doc
4. **GitHub 文档**: https://docs.github.com

### 社区支持

1. **Streamlit 论坛**: https://discuss.streamlit.io
2. **GitHub Issues**: 在项目仓库中提交问题
3. **Stack Overflow**: 搜索相关问题

### 联系支持

1. **Streamlit Cloud 支持**: 通过 Streamlit Cloud 控制台联系
2. **GitHub Support**: https://support.github.com

---

## 🎉 恭喜！

您已经成功部署了 Streamlit 应用到 Streamlit Cloud！

现在您可以：
- ✅ 随时随地访问您的应用
- ✅ 分享应用 URL 给其他人
- ✅ 持续更新和维护应用
- ✅ 享受免费的云托管服务

---

**祝您使用愉快！** 🚀

如有任何问题，请随时查看文档或联系支持。

